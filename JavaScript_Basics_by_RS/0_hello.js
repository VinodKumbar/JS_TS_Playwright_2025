console.log("Hello, World!");

// this is a simple JavaScript file that prints "Hello, World!" to the console.
// You can run this code in a JavaScript environment like Node.js or a web browser console. 
/*
node hello.js
 */



let x = 10;

for(let i = 0; i < 10; i++) {
    console.log("Value of x is:", x);
    x += 5; // Incrementing x by 5 in each iteration
}   

console.log("Final value of x is:", x); // Displaying the final value of x after the loop
// This code demonstrates a simple loop that increments a variable and prints its value.




let y = 20;
for(let j = 0; j < 5; j++) {
    console.log("Value of y is:", y);
    y -= 2; // Decrementing y by 2 in each iteration
}


let z = 30;
for(let k = 0; k < 3; k++) {
    console.log("Value of z is:", z);
    z *= 2; // Multiplying z by 2 in each iteration
}